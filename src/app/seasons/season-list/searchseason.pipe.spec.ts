import { SearchseasonPipe } from './searchseason.pipe';

describe('SearchseasonPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchseasonPipe();
    expect(pipe).toBeTruthy();
  });
});
